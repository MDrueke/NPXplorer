1. A mechanism to open a window to view the single channel waveform
2. Long - term: Option to read Allen Mouse Atlas, let the user enter lambda-bregma distance (scale the atlas to it), and the insertion coordinates, and then label areas according to the atlas as an overlay over the plotted ephys data.
3. Currently, right and left click label channels. Change this to Alt+(Left/Right Click). Right clicking a channel opens a context menu for this channel. Unpopulated for now.
4. SpikeGLX-like electrode selection interface that is a popup that shows the probe (one or multiple shanks) and lets the user left click on channels to include them, right click to exclude them. Can also hold left click and drag to draw a rectangle that selects multiple electrodes.
5. Bad channel detection
6. Surface detection
