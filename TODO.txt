
1. Long - term: Option to read Allen Mouse Atlas, let the user enter lambda-bregma distance (scale the atlas to it), and the insertion coordinates, and then label areas according to the atlas as an overlay over the plotted ephys data.

2. SpikeGLX-like electrode selection interface that is a popup that shows the probe (one or multiple shanks) and lets the user left click on channels to include them, right click to exclude them. Can also hold left click and drag to draw a rectangle that selects multiple electrodes.
3. IBL-style channel classification: Classify channels as dead/noise/out-of-brain/good. The default implementation draws repeated random 300 ms chunks across the recording and uses a majority vote over 100 chunks. The code is in /home/mdrueke/Code local/old/ibl-sorter. Find the relevant bits and implement them in rust in this app. This shouldn't run by default. There should be a "Channel Classification" button next to the other in the top right. If clicked, the classification is run. The results are overlayed over the heatmap. Each channel that wasn't classified as good gets a semitransparent (0.2 alpha) horizontal overlay in the color of its identified class: Dead - magenta. Noisy - red. out-of-brain - green. Let's try this and see how it looks. The user needs to be able to toggle this overlay on and off. Add a box in the top right within the heatmap that is a semitransparent (0.3 alpha) box (background color) without window decorations that has rounded corners and holds a legend for the overlay colors (no transparency) and a button (not transparency) "Disable Overlay". If that button is pressed, the overlay is disabled and the legend vanishes, with the box collapsing to now only encompass the button. The button's label changes to "Enable Overlay"
4. Surface detection
